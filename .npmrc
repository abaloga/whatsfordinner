# react-dom is an optional peer dep of expo-router used only for web rendering.
# We target iOS/Android only, so npm's strict peer-dep resolution would
# incorrectly reject packages. This flag restores npm v6 behaviour.
legacy-peer-deps=true
